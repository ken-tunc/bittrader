spring:
  application:
    name: bittrader-web
  main:
    web-application-type: reactive
  profiles:
    group:
      prod:
        - auth
      local:
        - dev
        - auth
      test:
        - dev
server:
  port: 8080
bittrader:
  webjars:
    versions:
      apexcharts: @apexchartsVersion@
      bootstrap: @bootstrapVersion@
